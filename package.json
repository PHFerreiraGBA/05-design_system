{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@phdesignsys/docs && changeset publish"
  },
  "devDependencies": {
    "changeset": "^0.2.6",
    "turbo": "^2.4.4"
  },
  "packageManager": "npm@10.8.1"
}
